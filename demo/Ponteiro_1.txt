int a;
a=10;
int b;
b=20;
int c.sub(b,a);

int sub(int a, int b){
  return a-b;
}

retorna 10 ou -10?
1. guarda A num endereço de memória
2. coloca um valor na memória guardada (espaço criado);
3. o mesmo acontece para b;
4. no caso de c, tbm é criado um endereço
de memória, mas aguarda a função
   - na função é criado um endereço para a e b;
   - quando ela é chamada para dentro do endereço
c, ela é populada com os valores passados como parâmetro;
   - assim, c é populada com o retorno da operação da função.
